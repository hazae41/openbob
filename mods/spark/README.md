# Spark

### Demo

Compile and deploy the module (it will display the module address)

```bash
npm run prepack && npm run produce
```

Get the balance of some account

```bash
npm run execute <module> balance text:<account>
```